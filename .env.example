# --- HiveMQ Cloud ---
# 1. Sign up/Login: https://console.hivemq.cloud
# 2. Create Cluster (Free) -> Manage Cluster -> Overview -> Copy "Host" (without port/protocol)
HIVEMQ_HOST=xxxxxxx.s1.eu.hivemq.cloud

# 3. Access Management -> Create NEW credentials (Username/Password).
# DO NOT use your HiveMQ Console login! Create a specific user for the device.
HIVEMQ_USERNAME=device-user-01
HIVEMQ_PASSWORD=secure-password-123
HIVEMQ_PORT=8883

# --- Supabase ---
# 1. Sign up/Login: https://supabase.com
# 2. New Project -> Project Settings (Gear Icon) -> API
# 3. Project URL -> Copy into SUPABASE_URL
SUPABASE_URL=https://xxxxxxx.supabase.co

# 4. Project API Keys -> Copy "service_role" key (secret) or "anon" key (public).
# Note: For backend scripts, "service_role" is easiest to bypass RLS issues.
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
